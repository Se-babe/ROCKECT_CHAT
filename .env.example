# Rocket.Chat server
ROOT_URL=http://localhost:3000
HOST_PORT=3000

# MongoDB image (default works on CPUs without AVX; use mongo:7.0 on AVX-capable hosts)
# MONGODB_IMAGE=mongo:7.0

# Rocket.Chat image (8.x needs MongoDB 8+; 7.x works with MongoDB 6.2 no-AVX)
# ROCKETCHAT_IMAGE=registry.rocket.chat/rocketchat/rocket.chat:7.9.3

# App deployment (used by scripts/deploy-app.sh)
RC_URL=http://localhost:3000
RC_USERNAME=admin
RC_PASSWORD=change-me

# Claude API (configure in Admin > Apps > UgaJapa Translation after deploy)
ANTHROPIC_API_KEY=
